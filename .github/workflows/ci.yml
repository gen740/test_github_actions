
name: Stylua check
on: [push]
jobs:
  Stylua:
    runs-on: ubuntu-latest
    steps:
      - name: StyLua
        uses: JohnnyMorganz/stylua-action@v1.1.1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          version: latest
          args: --check .
