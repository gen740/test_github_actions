name: Stylua check
on: [push]
jobs:
  Clang_test:
    runs-on: ubuntu-latest
    steps:
      - name: Clang
        working-directory: .
        run: ls
      - name: Clang
        working-directory: .
        run: make test
