name: Stylua check
on: [push]
jobs:
  Stylua:
    runs-on: self-hosted
    steps:
      - name: Clang
        uses: egor-tensin/setup-clang@v1
        with:
          version: latest
          platform: x64
